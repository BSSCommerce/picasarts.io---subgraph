specVersion: 0.0.2
schema:
  file: ./schema.graphql
dataSources:
  - kind: near
    name: Contract
    network: near-testnet
    source:
      account: "market.dungbss.testnet"
    mapping:
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      entities:
        - ExampleEntity
      receiptHandlers:
        - handler: handleReceipt
      file: ./src/mapping.ts
